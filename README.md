# Ready to Reply

Site de présentation de Ready to Reply, en un seul fichier.

Le fichier publié par Netlify est `ready-to-reply/en-ligne/index.html`
(le projet Netlify `readytoreply` utilise ce dossier comme base, sans
commande de build). Chaque envoi sur `main` met le site à jour
automatiquement.
